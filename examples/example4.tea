myCoolArray = {
    'name': 'Mike',
    'age': getAge()
}

otherArray = [1, 2, 3]

callFunc([1, 2, 3])

emptyArr = []

if not (a > 10) 
    echo('A is not greater than 10')
end

myValue = not myVariable

